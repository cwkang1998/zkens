VITE_FORCE_CHAIN=local
VITE_RPC_HTTP_LOCAL=http://127.0.0.1:8545
# Optional: override backend proxy target if not 3001
# VITE_API_URL=http://localhost:3001

# For local dev, Events panel will use contracts/deployments/local.json for pool address
# You can still override it explicitly if desired:
# VITE_POOL_ADDRESS=0xYourLocalPoolAddress

